# Your ML / AI Tutor - shared requirements
#
# Phase 0 lessons (001-...) use only the Python standard library.
# You do NOT need to install anything to run them. Python 3.10+ is enough.
#
# The pins below are for later phases. Install them when the lesson you are
# on tells you to:
#
#     python -m venv .venv
#     source .venv/bin/activate        # Windows: .venv\Scripts\activate
#     pip install -r requirements.txt
#
# Versions are pinned to a known-good minor range so lessons keep working.

# Phase 1 - data science basics
numpy>=1.26,<3
pandas>=2.2,<3
matplotlib>=3.8,<4
scipy>=1.12,<2

# Phase 2 - classical machine learning
scikit-learn>=1.4,<2

# Phase 3 - deep learning foundations
# (CPU wheels are fine for every lesson in this course.)
torch>=2.2,<3

# Phase 4 - modern AI (NLP, vision, generative / LLM basics)
# These are heavier; the Phase 4 lessons will tell you when to install them.
# transformers>=4.40,<5
# datasets>=2.19,<4
# pillow>=10.3,<12

# Nice-to-have for exploring interactively (optional)
# jupyterlab>=4.1,<5
